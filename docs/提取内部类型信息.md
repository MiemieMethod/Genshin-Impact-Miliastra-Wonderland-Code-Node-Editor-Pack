composite 节点的两个部分: 外壳与内核是完全分离的, 因此可以仅操作外部接口定义, 在服务器节点图中引用, 就可以得到具有良好类型定义的接口了......


枚举类型是完全通用的, 内核中仅使用枚举值(int)进行运算, 但是外壳的 API 接口需提供特定类型, 而被提供的类型也会在连线时进行选择, 
相同类型的才可以互联. 两个不同的类型可以包含相同的枚举字段(但是UI显示可能不同, 我们这里不做区分)

